# 写作规范（供所有作者/协作代理使用）

本规范适用于《Nix 与 NixOS 中文手册》全部章节，任何一章在动笔前必须先通读本规范。

## 语言与风格

1. 全书使用**简体中文**，技术名词首次出现时保留英文原文，例如「派生（derivation）」「属性集（attribute set）」。此后可单独使用中文或英文，保持一章内一致。
2. 语气：亲切但精确。面向有 Linux 使用经验、但可能是 Nix 零基础的读者。允许适度比喻（如「/nix/store 是一座图书馆」），但比喻之后必须给出严格定义。
3. 不使用「显然」「很简单」等词。难点要正面解释，不回避。
4. 避免空洞的赞美（「Nix 非常强大」）。每个优点都要配一个具体场景或反例（传统发行版怎么做、为什么会坏、Nix 怎么解决）。

## 章节结构

每章固定结构：

```markdown
# 第 N 章 标题

> **本章导读**：两到四句话，说明本章回答什么问题、在全书中的位置。

（正文，若干节 `## N.1`、`## N.2` ...）

## N.x 本章小结

- 要点列表（5–8 条）

## 延伸阅读

- 链接 + 一句话说明
```

小节编号采用 `N.M` 形式（如 `3.2`）。章内代码块、表格、图（ASCII 图）按需使用，不强制编号。

## 代码块

1. 代码块必须标注语言（nix / bash / console / ini 等）。
2. **逐行注释**是本书的招牌：对 nixpkgs 真实源码的讲解，几乎每一行（或每个逻辑块）都要跟一条中文注释，解释「这一行为什么存在、删掉会怎样、对应什么机制」。注释写在代码内 `#` 或代码块下方逐段讲解均可，关键代码优先代码内注释 + 代码后逐段精讲。
3. 命令示例：`$` 开头为普通用户命令，`#` 开头为 root 命令，标注语言为 `console`，且命令与输出分开：

```console
$ nix-store -r /nix/store/...-hello-2.12.3
/nix/store/...-hello-2.12.3
```

4. 涉及 nixpkgs 真实源码时，标注仓库内相对路径与来源（如 `pkgs/by-name/he/hello/package.nix`，master 分支 2026-08 快照）；若为教学简化版，必须注明「简化版」。
5. 代码中的字符串、变量名、路径保持英文；注释用中文。

## 版本与事实基准（2026 年 8 月）

- NixOS 当前稳定版：**26.05 "Yarara"**（2026-05-30 发布，支持至 2026-12-31）；上一稳定版 25.11 "Xantusia"。
- Nix 包管理器稳定版：**2.35**（2026-06 发布）。
- Nix 历史关键点：2003 年乌得勒支大学项目启动；2006 年 Dolstra 博士论文；2006-06-03 NixOS 原型（Armijn Hemel）；2013-12-01 首个稳定版 13.10 "Aardvark"；2018-02 Nix 2.0（新 CLI）；2021-11 Nix 2.4（Flakes 测试期）；2024 社区分叉与治理改革（Lix、Tvix、Determinate Systems 兴起）。
- 代号按字母序的动物命名：13.10 Aardvark → 14.04 Baboon → 14.12 Caterpillar → 15.09 Dingo → 16.03 Emu → 16.09 Flounder → 17.03 Gorilla → 17.09 Hummingbird → 18.03 Impala → 18.09 Jellyfish → 19.03 Koi → 19.09 Loris → 20.03 Markhor → 20.09 Nightingale → 21.05 Okapi → 21.11 Porcupine → 22.05 Quokka → 22.11 Raccoon → 23.05 Stoat → 23.11 Tapir → 24.05 Uakari → 24.11 Vicuña → 25.05 Warbler → 25.11 Xantusia → 26.05 Yarara。
- nixpkgs 新包一律放 `pkgs/by-name/<双字母前缀>/<包名>/package.nix`（RFC 140），无需再改 `all-packages.nix`。
- 不确定的细节宁可写「以官方手册为准」并给链接，不要编造版本号、日期或选项名。

## 篇幅

- 每章正文（不含代码）至少 3000 字，含代码总计 400–800 行 markdown。
- 代码占比高的章节（打包实例）可以更长，不设上限。

## 交叉引用

- 引用其他章写「参见第 N 章」，不使用相对链接（便于后续导出 PDF）。
- 章末「延伸阅读」给官方链接：nixos.org、nix.dev、nixos.org/manual、wiki.nixos.org、GitHub。
